# API Configuration (same as backend)
# Production: set PUBLIC_HOST (e.g., api.example.com)
# Cloud dev: leave empty, will auto-detect from APP_PORT + CLACKY_PREVIEW_DOMAIN_BASE
# Local dev: leave empty, will use http://localhost:3000
PUBLIC_HOST=
